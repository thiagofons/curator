# =============================================================================
# CMS — Secret environment variables
# Copy to secrets.env and fill in real values (never commit secrets.env)
# =============================================================================

# Database (Supabase connection string for cms schema)
DATABASE_CMS_URL=postgresql://cms_user:password@host:5432/postgres

# Payload CMS
PAYLOAD_SECRET=change-me-payload-secret
PAYLOAD_SERVER_URL=https://cms.curator.com.br
