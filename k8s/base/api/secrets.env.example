# =============================================================================
# API — Secret environment variables
# Copy to secrets.env and fill in real values (never commit secrets.env)
# =============================================================================

# Database (Supabase connection string for api schema)
DATABASE_URL=postgresql://api_user:password@host:5432/postgres

# Redis
REDIS_URL=redis://:password@redis:6379

# Auth
JWT_SECRET=change-me-jwt-secret

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_SECRET=your-google-secret
GOOGLE_CALLBACK_URL=https://api.curator.com.br/auth/google/callback
