# =============================================================================
# Redis — Secret environment variables
# Copy to secrets.env and fill in real values (never commit secrets.env)
# =============================================================================

REDIS_PASSWORD=change-me-redis-password
