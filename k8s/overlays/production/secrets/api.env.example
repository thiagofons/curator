# =============================================================================
# API — Production secrets
# Copy to api.env and fill in values (never commit api.env)
# Created by CD pipeline from GitHub Secrets
# =============================================================================

# Supabase connection string (api schema via PgBouncer)
DATABASE_URL=postgresql://api_user:password@db.supabase.co:6543/postgres

# Redis (local K3s pod)
REDIS_URL=redis://:password@redis:6379

# Auth
JWT_SECRET=production-jwt-secret

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_SECRET=your-google-secret
GOOGLE_CALLBACK_URL=https://api.curator.com.br/auth/google/callback
