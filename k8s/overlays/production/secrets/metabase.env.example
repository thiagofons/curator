# =============================================================================
# Metabase — Production secrets
# Copy to metabase.env and fill in values (never commit metabase.env)
# Created by CD pipeline from GitHub Secrets
# =============================================================================

# Supabase connection string (crm schema)
MB_DB_CONNECTION_URI=postgresql://crm_user:password@db.supabase.co:6543/postgres

# Metabase encryption key (generate with: openssl rand -hex 32)
MB_ENCRYPTION_SECRET_KEY=production-metabase-secret-key
