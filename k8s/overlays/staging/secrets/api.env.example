# =============================================================================
# API — Staging secrets
# Copy to api.env and fill in values (never commit api.env)
# =============================================================================

# Local postgres (created by postgres StatefulSet in staging overlay)
DATABASE_URL=postgresql://api_user:password@postgres:5432/curator_api

# Redis (local, running in K3s)
REDIS_URL=redis://:redis_password@redis:6379

# Auth
JWT_SECRET=staging-jwt-secret-change-me

# Google OAuth (use test credentials for staging)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_SECRET=your-google-secret
GOOGLE_CALLBACK_URL=http://api.curator.local/auth/google/callback
