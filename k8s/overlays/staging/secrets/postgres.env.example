# =============================================================================
# PostgreSQL — Staging secrets
# Copy to postgres.env and fill in values (never commit postgres.env)
# =============================================================================

POSTGRES_USER=curator
POSTGRES_PASSWORD=staging_db_password
POSTGRES_DB=curator
