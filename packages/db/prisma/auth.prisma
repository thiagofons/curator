// O "engineType" que antigamente ficava aqui, agora é gerenciado pelo prisma.config.ts!
generator client {
  provider = "prisma-client-js"
  output   = "../src/auth/client"
}

generator zod {
  provider = "prisma-zod-generator"
  output   = "../../../packages/model/src/generated/auth"
}

datasource authDb {
  provider = "postgresql"
}

model User {
  id    String @id @default(uuid())
  email String @unique
}
