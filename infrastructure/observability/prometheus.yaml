global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # TODO: Adicionar aqui os 'targets' dos seus microsserviços NestJS
  # ex: - targets: ['host.docker.internal:8080'] # Para seu 'identity-api'
