# ===================================================================
# State Files - Security Pillar (DevSecOps)
# NEVER commit state, especially from the /bootstrap folder.
# The state file contains a map of your infra and can hold secrets.
# ===================================================================
*.tfstate
*.tfstate.backup


# ===================================================================
# Plugin & Module Directory - DX Pillar (Developer Experience)
# This folder is generated by "terraform init".
# It is specific to your operating system and should not be committed.
# ===================================================================
.terraform/


# ===================================================================
# Execution Plans - Security Pillar (DevSecOps)
# Saved plans (e.g., terraform plan -out=my_plan) contain
# the full infrastructure state and potential secrets.
# ===================================================================
*.tfplan


# ===================================================================
# Local Variable Files - Security Pillar (DevSecOps)
# Files that override variables, commonly used for local secrets
# (e.g., API keys for testing).
# ===================================================================
*.auto.tfvars
*.auto.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json


# ===================================================================
# Error Logs & Local Settings
# ===================================================================
crash.log
terraform-provider-*.log
.terraformrc
terraform.rc